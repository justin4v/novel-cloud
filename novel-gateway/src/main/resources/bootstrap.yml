
spring:
  application:
    name: novel-gateway
  cloud:
    nacos:
      config:
        server-addr: novel-nacos:8848
        file-extension: yml
        group: ${spring.application.name}
        namespace: c8db504f-ac2f-4bed-91fb-5d7b8c3543b7
    #关闭Spring自带的X-Forwarded-Prefix设置
    gateway:
      x-forwarded:
        prefix-enabled: false

  main:
    allow-bean-definition-overriding: true

